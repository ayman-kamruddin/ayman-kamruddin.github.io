Simple webpage published through GitHub.

Book-review pages are generated from `templates/book_template.html` and
`books/reviews.json`. After changing either, run `python3 scripts/build_book_reviews.py`.
